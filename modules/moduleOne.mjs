const plus = (x, y) => x + y;

const SomeName = 'Alex';

const minus = (t, r) => t - r;

const mult = (a, b) => a * b;


export {SomeName, plus, minus, mult};

//export default plus; // если пересылать только одну переменную то можно использовать  default - а это в свою очередь позволет мне писать названия перменных в файлах по разному - в случае если один экспорт

//функция экспортируется
